class BotWords:

    #def __init__(self, message):

    def report(message):

        # Привет
        if message == "Привет":
            return f"Привет-привет!"

        # Пока
        elif message == "Пока":
            return f"Пока-пока!"

        else:
            return "Нуль"